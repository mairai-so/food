import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.miarai.food.equipe',
  appName: 'MIAR Equipe',
  webDir: 'dist/public',
  server: { androidScheme: 'https' },
};

export default config;
