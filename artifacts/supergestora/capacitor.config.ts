import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.miarai.food.supergestora',
  appName: 'MIAR Supergestora',
  webDir: 'dist/public',
  server: { androidScheme: 'https' },
};

export default config;
